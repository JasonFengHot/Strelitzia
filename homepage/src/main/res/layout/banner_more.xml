<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false">
        <Button
            android:id="@+id/get_more_btn"
            android:layout_width="@dimen/more_btn_width"
            android:layout_height="@dimen/more_btn_height"
            android:gravity="center"
            android:text="更多精彩"
            android:background="@drawable/banner_more_selector"
            android:layout_marginTop="@dimen/more_btn_margin_top_bottom"
            android:layout_marginBottom="@dimen/more_btn_margin_top_bottom"
            android:layout_centerHorizontal="true"
            android:textColor="#ffffff"
            android:textSize="30sp" />
</RelativeLayout>