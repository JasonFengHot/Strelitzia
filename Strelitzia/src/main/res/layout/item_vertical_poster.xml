<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
     android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:focusableInTouchMode="true"
    android:focusable="true"
    android:paddingRight="@dimen/item_vertical_pr"
    >

    <RelativeLayout
        android:layout_width="@dimen/item_vertical_w"
        android:layout_height="@dimen/item_vertical_h"
        >

    <ImageView
        android:id="@+id/item_vertical_poster_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/_333333"
        />
    <TextView
        android:id="@+id/item_vertical_poster_title"
        android:layout_width="@dimen/item_vertical_w"
        android:layout_height="@dimen/item_vertical_title_h"
        android:background="@drawable/poster_bottom_layer"
        android:textSize="@dimen/item_vertical_title_ts"
        android:textColor="#f0f0f0"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:gravity="bottom|center_horizontal"
        android:paddingBottom="@dimen/item_vertical_title_pb"
        android:layout_alignParentBottom="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:visibility="gone"/>
    </RelativeLayout>
</RelativeLayout>