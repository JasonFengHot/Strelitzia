<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tv.ismar.subject">


    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".SubjectActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/IsmartvAppTheme.Fullscreen">
            <intent-filter>
                    <action android:name="tv.ismar.daisy.subject" />
                    <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
